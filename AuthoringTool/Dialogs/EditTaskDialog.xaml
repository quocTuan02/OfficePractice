<Window x:Class="AuthoringTool.Dialogs.EditTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm / Sửa Task"
        Width="520" Height="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#1E1E2E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Mục tiêu (Objective)" FontWeight="SemiBold"/>
        <TextBox Grid.Row="2" x:Name="TxtObjective" />

        <TextBlock Grid.Row="4" Text="Hướng dẫn cho thí sinh (Instruction) *" FontWeight="SemiBold"/>
        <TextBox Grid.Row="6" x:Name="TxtInstruction"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 Height="100" VerticalScrollBarVisibility="Auto"/>

        <TextBlock Grid.Row="8" Text="File Template" FontWeight="SemiBold"/>
        <Grid Grid.Row="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="TxtTemplateFile" IsReadOnly="True"/>
            <Button Grid.Column="2" Content="Chọn..." Click="Browse_Click"
                    Background="#45475A" Foreground="#CDD6F4"/>
        </Grid>

        <TextBlock Grid.Row="12" Text="Điểm (Points)" FontWeight="SemiBold"/>
        <TextBox Grid.Row="14" x:Name="TxtPoints" Text="10" Width="100" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="16" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Hủy" Click="Cancel_Click" Background="#45475A" Foreground="#CDD6F4"/>
            <Button Content="  Lưu  " Click="Ok_Click" Margin="8,0,0,0"
                    Background="#89B4FA" Foreground="#1E1E2E" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
